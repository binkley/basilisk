basilisk:
  extra-word: 'Uncle Bob'

logging:
  level:
    root: 'WARN'

management:
  endpoints:
    web:
      exposure:
        include: '*'

server:
  error:
    include-exception: true
    include-stacktrace: never

spring:
  application:
    name: 'basilisk'
  main:
    banner-mode: 'off'

---

spring:
  profiles:
    active: 'alternative'

basilisk:
  extra-word: 'Margaret Hamilton'
